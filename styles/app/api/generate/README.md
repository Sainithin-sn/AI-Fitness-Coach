# AI Fitness Coach

AI-powered fitness assistant built with Next.js. Generates personalized workout and diet plans using LLMs, with voice (Web Speech API), on-the-fly images (Pollinations), and PDF export.

## Run (in GitHub Codespaces or Vercel)
- Add OPENAI_API_KEY to environment.
- npm ci && npm run dev (in Codespaces)
